{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell",
  "description": "Capability for shell operations",
  "windows": ["main"],
  "permissions": [
    "shell:allow-execute",
    "shell:allow-open"
  ],
  "platforms": ["macOS", "linux", "windows"]
}