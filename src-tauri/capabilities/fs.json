{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "fs",
  "description": "Capability for file system operations",
  "windows": ["main"],
  "permissions": [
    "fs:allow-read",
    "fs:allow-write",
    "fs:allow-create",
    "fs:allow-remove"
  ],
  "platforms": ["macOS", "linux", "windows"]
}